name: CI/CD

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  # Required for vite build (client-side)
  VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
  VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
  VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
  VITE_EMAILJS_SERVICE_ID: ${{ secrets.VITE_EMAILJS_SERVICE_ID }}
  VITE_EMAILJS_TEMPLATE_ID: ${{ secrets.VITE_EMAILJS_TEMPLATE_ID }}
  VITE_EMAILJS_CONTACT_TEMPLATE_ID: ${{ secrets.VITE_EMAILJS_CONTACT_TEMPLATE_ID }}
  VITE_EMAILJS_PUBLIC_KEY: ${{ secrets.VITE_EMAILJS_PUBLIC_KEY }}
  VITE_RECAPTCHA_SITE_KEY: ${{ secrets.VITE_RECAPTCHA_SITE_KEY }}
  RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
  ALERT_EMAIL: ${{ secrets.ALERT_EMAIL }}
  # Required for server-side API (used at runtime, not build time)
  GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
  GOOGLE_SERVICE_EMAIL: ${{ secrets.GOOGLE_SERVICE_EMAIL }}
  GOOGLE_PRIVATE_KEY: ${{ secrets.GOOGLE_PRIVATE_KEY }}
  RECAPTCHA_SECRET_KEY: ${{ secrets.RECAPTCHA_SECRET_KEY }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci

      # Step 1: Lint check
      - name: Lint
        run: npm run lint

      # Step 2: Format check
      - name: Format check
        run: npm run format:check

      # Step 3: TypeScript check
      - name: TypeScript check
        run: npm run typecheck

      # Step 5: Unit tests
      - name: Run unit tests
        run: npm test

      # Step 6: Build (also validates env vars via verify-env.mjs)
      - name: Build production bundle
        run: npm run build

  deploy:
    # Only deploy on push to main (not PRs)
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: test
    permissions:
      contents: read
      actions: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci

      # Step 3: Build for deploy
      - name: Build production bundle
        run: npm run build

      # Step 4: Install Puppeteer for E2E tests
      - name: Install Puppeteer
        run: npm install --no-save puppeteer

      # Step 5: Start preview server for E2E tests
      - name: Start preview server
        run: |
          npx vite preview --port 3002 &
          sleep 3
          echo "Preview server started on port 3002"

      # Step 6: Run E2E tests against preview server
      - name: Run E2E tests
        run: node scripts/deploy-e2e.mjs http://localhost:3002

      # Step 7: Visual preview check (2 mobile screenshots)
      - name: Visual preview check
        env:
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          SCREENSHOT_RECIPIENT: ${{ vars.NOTIFICATION_EMAIL }}
        run: |
          npx playwright install chromium
          node scripts/preview-check.mjs http://localhost:3002

      # Step 8: Upload preview screenshots as artifacts
      - name: Upload preview screenshots
        uses: actions/upload-artifact@v4
        with:
          name: preview-screenshots
          path: screenshots/
          retention-days: 14

      # Step 9: Build Vercel output for production (creates .vercel/output/ from dist/)
      - name: Build Vercel output
        run: npx vercel@54.9.1 build --prod --yes --token=${{ secrets.VERCEL_TOKEN }}

      # Step 10: Deploy to Vercel production (prebuilt — uses .vercel/output/)
      - name: Deploy to Vercel
        run: npx vercel@54.9.1 deploy --prebuilt --prod --yes --token=${{ secrets.VERCEL_TOKEN }}

      # Step 11: Smoke test against production
      - name: E2E smoke test on production
        run: |
          sleep 10  # Wait for Vercel to propagate
          node scripts/deploy-e2e.mjs https://abhayandrebecca.com

